package sync_balance_test

import (
	"testing"
)

func TestGetINTMAXTokenAddress(t *testing.T) {
	t.Parallel()

	// var (
	// 	tokenType    uint8 = 0
	// 	tokenAddress       = common.Address{}
	// 	tokenID            = big.NewInt(0)
	// )

	// sync_balance.GetTokenIndex(tokenType, tokenAddress, tokenID)
}
